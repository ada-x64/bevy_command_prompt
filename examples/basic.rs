use bevy::prelude::*;
use bevy_ui_console::{ConsolePlugin, ui::Console};

pub fn main() {
    // let mut app = App::new();
    // app.add_plugins(DefaultPlugins);
    // app.add_plugins(ConsolePlugin);
    // app.add_systems(Startup, |mut commands: Commands| {
    //     debug!("um hello");
    //     commands.spawn(Camera2d);
    //     commands.spawn(Console);
    // });
    // app.run();
}
